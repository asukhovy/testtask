To run the tests, follow these steps:
1. Go to the folder with the project
2. Executes the console command "mvn test".

To build the project, follow these steps:
1. Go to the folder with the project
2. Executes the console command "mvn package".
3. Maven creates .jar file with name com.codenvy.testtask.qname in "/target" folder